# Copilot Instructions

This is a Formbricks (survey & forms) template. Check ghcr.io/formbricks/formbricks image tags, NextAuth + PostgreSQL config.

## When checking for updates
- Check if Docker image has newer stable tags
- Verify environment variables match current version
- Check for new required configuration
- Keep the template minimal and deployment-ready